# Public origin of the deployed site. Used for canonical URLs, Open Graph tags, sitemap.xml, robots.txt and JSON-LD.
# Leave unset in development (defaults to http://localhost:3000).
NEXT_PUBLIC_SITE_URL=https://your-domain.example
